# 用户登录 / 授权 Demo

## 1. 项目简介

本项目是一个基于 Web 的用户登录与资源授权 Demo，主要实现用户注册、登录、JWT 身份认证、RBAC 权限控制以及 LLM 用户名审核。

核心权限设计：

| 角色    | Resource A | Resource B |
| ----- | ---------: | ---------: |
| USER  |          ✅ |          ❌ |
| ADMIN |          ✅ |          ✅ |

普通用户注册后默认只能访问 Resource A，Resource B 仅授权用户可访问。同时在注册环节增加 LLM 用户名审核。

---

## 2. 时间规划

主要花了大概两天的时间。

### 29号晚上

对 Demo 需求进行拆解和整体规划，明确功能、技术方案和开发流程，并整理 `AGENTS.md` 作为 AI Coding Agent 的开发约束。

### 30号：后端 + AI + 前端

完成：

- 用户注册、登录
- JWT 认证
- RBAC 权限控制
- Resource A / B
- LLM 用户名审核
- Prompt 调试
- 前端登录、注册和 Dashboard
- 前后端联调

### 31号：部署与打磨

完成：

- UI 和交互优化
- 异常处理
- Docker 部署
- Nginx 配置
- 公网访问
- 完整测试
- 项目文档整理

---

## 3. 实现方式

后端采用 FastAPI，使用 MySQL 存储用户数据，JWT 实现身份认证，并通过 RBAC 控制不同角色的资源访问权限。

用户注册时先进行基础参数校验，再调用 LLM 对用户名进行社区规范审核。审核结果采用结构化输出，由后端根据结果决定是否创建用户。

密码使用 Hash 存储，不保存明文密码；Resource B 的权限判断由后端完成，前端只负责展示对应状态。

---

## 4. 整体架构与技术栈

整体采用前后端分离架构：

```text
用户
 ↓
Nginx
 ↓
Vue 3 前端
 ↓
FastAPI 后端
 ├── MySQL
 ├── JWT / RBAC
 └── LLM 用户名审核
```

### 技术栈

**前端**

- Vue 3
- TypeScript
- Vite
- Vue Router
- Axios
- Naive UI

**后端**

- Python
- FastAPI
- SQLAlchemy
- MySQL
- JWT
- pwdlib / bcrypt
- LLM API

**部署**

- Docker
- Docker Compose
- Nginx

设计上没有引入 Redis、消息队列、微服务等额外组件，主要考虑到项目只有 3 天开发周期，优先保证核心功能完整、架构清晰和能够稳定部署。

目前项目已通过 Docker + Nginx 部署到服务器并提供公网 HTTP 访问。由于本项目定位为 Demo，当前未申请 SSL 证书，也未配置 HTTPS。

---

## 5. LLM 审核与调试

LLM 主要用于判断注册用户名是否存在明显的社区违规内容。

最初采用 Zero-shot Prompt，测试后发现部分边界用户名判断不够稳定，因此逐步进行了优化：

```text
Zero-shot
 ↓
增加明确的违规分类和判断规则
 ↓
增加 Few-shot 示例
 ↓
Structured Output
```

通过实际测试不断调整 Prompt，使模型输出更加稳定，同时降低正常用户名被误判的情况。

在部署测试过程中还遇到过模型访问量过大导致的 `429`，以及请求超时问题，因此在实现中增加了异常处理，LLM 审核失败时不会直接创建用户，而是提示用户稍后重试。

---

## 6. AI Coding 工具使用情况

本项目主要使用 **ZCode** 进行 AI Coding，同时结合实际运行环境进行代码调试和问题排查。

AI 主要参与：

- 项目结构和基础代码生成
- 用户认证与 RBAC
- LLM 接入和 Prompt 设计
- 前端页面实现
- Docker 部署
- Bug 定位与修复

按照 ZCode 面板统计，本项目大约消耗 **800 万 Token**。

Token 消耗较多的部分主要是：

1. 前后端联调
2. LLM 审核模块反复调试
3. Docker / 公网部署问题排查

---

## 7. 自己时间花费最多的部分

不计算 AI 生成代码的时间后，我投入时间最多的部分主要是：

**环境联调和部署问题排查。**

尤其是 Docker 环境变量、部署后的实际运行情况以及LLM审核调试。这些问题需要结合真实环境日志进行定位，无法完全依赖代码生成。

---

## 8. 我认为该场景下优先级最高的部分

我认为该项目优先级最高的是：

> **认证和授权逻辑必须正确 ( P0 )**

因为这个 Demo 的核心不是“做出一个登录页面”，而是验证：

```text
USER
 ├── Resource A ✅
 └── Resource B ❌

ADMIN
 ├── Resource A ✅
 └── Resource B ✅
```

其中权限判断必须由后端完成。

在这个场景下，我的优先级排序是：

```text
认证 / 授权
    ↓
核心功能完整
    ↓
公网可访问
    ↓
AI 审核
    ↓
异常处理与用户体验
    ↓
UI 美观和额外功能
```

整体设计遵循一个原则：

> **在有限时间内，不追求复杂，而是尽可能把一个 Demo 做得完整、可靠并具备真实产品应有的基本体验。**

---

## 9. 项目最终状态

目前项目已完成：

- 用户注册 / 登录
- JWT 身份认证
- USER / ADMIN 权限控制
- Resource A / B
- LLM 用户名审核
- 前后端联调
- Docker 部署
- 公网访问

并提供一个可访问 Resource B 的测试账号用于验收。

\
访问地址 : [http://47.104.139.0:8080/](http://47.104.139.0:8080/)

测试账号 : username : admin

                 password : admin12345 

